export function chapter_xcmTransaction(element: HTMLDivElement) {
  element.innerHTML = `
    <h3>Chapter 10 - XCM Transaction</h3>
    <div class="list"</div>
      <ol>
        <li>create a new typescript file. Call it xcm.ts</li>
        <li>Connect to your local node.</li>
        <li>Create a function with takes a div as an input</li>
        <li>Copy Paste chapter 6 function to make a successful xcm call.</li>
        <li>Create  function which takes if balances is diffrent, then before.</li>
        <li>Change the inner HTML after sucessfull calling to a 🎉</li>
      </ol>
    </div>
  `;
}
