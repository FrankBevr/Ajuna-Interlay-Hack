export function chapter_xcmTransaction(element: HTMLDivElement) {
  element.innerHTML = `
    <h3>Chapter 10 - XCM Transaction</h3>
    1. create a new typescript file. Call it xcm.ts
    2. Connect to your local node.
    3. Create a function with takes a div as an input
    4. Copy Paste chapter 6 function to make a successful xcm call.
    5. Create  function which takes if balances is diffrent, then before.
    6. Change the inner HTML after sucessfull calling to a 🎉
  `;
}
